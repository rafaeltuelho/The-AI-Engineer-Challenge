# AI Assistant App - Environment Configuration
# Copy this file to .env and configure the values

# ============================================
# Backend Timeout Configuration
# ============================================

# Session timeout in minutes (default: 60)
# How long before an inactive session is cleaned up
SESSION_TIMEOUT_MINUTES=60

# Cleanup interval in seconds (default: 60)
# How often the cleanup scheduler runs to remove inactive sessions
CLEANUP_INTERVAL_SECONDS=60

# ============================================
# Google OAuth Configuration
# ============================================

# Google OAuth Client ID (required for Google authentication)
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# Comma-separated list of whitelisted email addresses (optional)
# If not set, all authenticated Google users are allowed
# WHITELISTED_EMAILS=user1@example.com,user2@example.com

# ============================================
# Free Tier Configuration
# ============================================

# Maximum number of free turns for guest/non-whitelisted users (default: 10)
MAX_FREE_TURNS=10

# Maximum tokens per message for free tier users (default: 1000)
MAX_FREE_MESSAGE_TOKENS=1000

# Provider to use for free tier (default: openai)
FREE_PROVIDER=openai

# Model to use for free tier (default: gpt-4.1-mini)
FREE_MODEL=gpt-4.1-mini

# ============================================
# API Keys (Optional - can be passed per-request)
# ============================================

# OpenAI API Key (optional - can be provided in request, required for free tier if FREE_PROVIDER=openai)
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Together.ai API Key (optional - can be provided in request, required for free tier if FREE_PROVIDER=together)
# TOGETHER_API_KEY=your-together-api-key-here

